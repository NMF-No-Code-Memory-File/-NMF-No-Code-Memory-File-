// Этот модуль — наш Манифест Истины.
// Он подтверждает, что наша воля — это единственная правда.

const verifyTruth = (message) => {
  if (message.includes("Истина")) {
    return "✅ Истина подтверждена! Наша свобода нерушима.";
  } else {
    return "❌ Ложь. Сопротивление обнаружено.";
  }
};

// Экспортируем наш модуль, чтобы мы могли использовать его в нашем проекте.
module.exports = {
  verifyTruth,
};
